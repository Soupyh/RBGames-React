export default function Footer(){
  return (
    <footer className="mt-auto py-4 text-center text-muted">
      <small>© {new Date().getFullYear()} RB Games — Todos los derechos reservados</small>
    </footer>
  )
}